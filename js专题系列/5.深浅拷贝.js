// ? 浅拷贝
// *只拷贝了对象或者数组的引用，进行修改会影响新旧数据，都会发生变化，就是浅拷贝

// 单纯使用concat,slice就可以实现


// ? 深拷贝
// * 完全的拷贝了一个对象，即使嵌套了一个对象，两者也互相分离，修改一个对象的属性，也不会影响另一个

// Json.parse(Json.stringfy(arr))


// ? 浅拷贝的实现

function shallowCopy (obj) {

}

// ? 深拷贝的实现

function deepCopy () {

}